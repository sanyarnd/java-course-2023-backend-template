app:
  telegram-token: ${TOKEN}
  scrapper: http://localhost:8080/

spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

springdoc:
  swagger-ui:
    path: /

server:
  port: 8090

logging:
  config: classpath:log4j2-plain.xml
